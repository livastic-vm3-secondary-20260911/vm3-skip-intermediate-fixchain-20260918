merge_queue:
  mode: serial
  max_parallel_checks: 2
  skip_intermediate_results: true

queue_rules:
  - name: vm3-skip-intermediate
    batch_size: 1
    batch_max_wait_time: 5 min
    queue_conditions: true
    merge_conditions:
      - check-success = @github-actions/vm3-skip-ci
    merge_method: merge
